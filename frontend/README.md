# Frontend

React frontend for the Laundry Manager Dashboard.

The current prototype uses React 18 through a CDN and does not require npm, Vite, or a build step.

## Main file

```text
frontend/index.html
```

## Notes

- Keep the frontend simple while the platform is still a local prototype.
- If the project grows, move to Vite + React with separate components.
